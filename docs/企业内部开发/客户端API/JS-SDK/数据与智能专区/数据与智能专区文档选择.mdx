---
title: "数据与智能专区文档选择"
generated_at: "2026-04-22 03:46:40"
update_time: 1776776399
source: "https://developer.work.weixin.qq.com/document/path/101499"
---

**接口说明**：调用该接口，可选择一个或多个自己创建的doc文档，选中后返回对应文档的docid与url，应用可在数据与智能专区中使用这些文档调用通用模型能力进行分析（灰度内测中）。

**调用前提：**
> 必须先使用ww.register进行应用身份注册。
>当前成员必须在应用的可见范围之中，否则调用时会报“no permission”错误。
>应用需具有数据与智能专区文档存档权限，且成员在该应用的文档存档范围内，否则调用时会报“no permission”错误。







#### 示例代码

```javascript
wx.invoke('selectDatazoneDoc', {
}, function (res) {
  if (res.err_msg == "selectDatazoneDoc:ok") {
		var docList = res.docList;
		for (var i = 0; i < docList.length; i++)
     	{
				var docid = docList[i].docid;
				var url = docList[i].url;
     	}
  }
});
```

#### 返回结果说明
| 参数名      |  类型    | 说明                               | 
| -------- | -------- | -------------------------------- | 
|res|Object|返回信息|
|res.err_msg|string|执行结果。"selectDatazoneDoc:ok" 表示执行成功，否则表示报错信息|
|res.docList|Array<object>|返回的文档列表|
|res.docList[].docid|string|选择的文档docid|
|res.docList[].url|string|选择的文档url|


**平台版本说明**

|平台|企业微信版本要求|
|--|--|
|ios|≥5.0.8|
|android|≥5.0.8|
|win|≥5.0.8|
|mac|≥5.0.8|

**应用权限说明**

|应用类型|可调用的应用|
|--|--|
|自建应用|需具有“数据与智能专区文档存档权限”（灰度内测中）|
|第三方应用|暂未支持|
|代开发应用|暂未支持|
