---
title: "接收消息"
generated_at: "2026-08-18 16:58:22"
update_time: 1786544727
source: "https://developer.work.weixin.qq.com/document/path/101842"
---

[TOC]

## 概述

当用户与智能机器人发生交互并发送消息时，企业微信会将消息加密后回调给机器人配置的接收消息 URL。开发者接收并处理回调消息，即可实现丰富的自定义功能。

目前支持触发消息回调的交互场景：

1. 用户群里@智能机器人 或单聊中向机器人发送**文本消息**
2. 用户群里 @智能机器人 或单聊中向机器人发送**图文混排消息**
3. 用户单聊中向机器人发送**图片消息**
4. 用户单聊中向机器人发送**语音消息**
5. 用户单聊中向机器人发送**本地文件消息**
6. 用户单聊中向机器人发送**视频消息**
7. 用户群里 @智能机器人 或单聊中向机器人发送**引用消息**

![](https://wework.qpic.cn/wwpic3az/697572_q7ZifS-8RiSJyHw_1750159499/0) ![](https://wework.qpic.cn/wwpic3az/108624_G2fA8Qr6TLSgnKp_1750159518/0)

交互流程如下：

![](https://wework.qpic.cn/wwpic3az/452410_NWYPvpVkTneezum_1750940240/0)

**流程说明：**

1. 当用户跟智能机器人交互发送支持的消息类型时，企业微信后台会向开发者后台推送[消息推送](#消息推送)。用户跟同一个智能机器人最多同时有三条消息交互中。
2. 开发者回调 URL 接收到新消息推送后：可选择生成[流式消息回复](#62165/流式消息回复)，并使用用户消息内容调用大模型/AI Agent；也可直接回复[模板卡片消息](#62160)。
3. 若开发者回复消息类型包含流式消息，企业微信在未收到流式消息回复结束前，会不断向开发者回调URL 推送[流式消息刷新](#流式消息刷新)（从用户发消息开始最多等待 10 分钟，超过 10 分钟结束推送）。开发者接收到流式消息刷新后，继续生成[流式消息回复](#62165/流式消息回复)。

> 接收消息与被动回复消息都是加密的，加密方式参考[加解密方案](#62162)。
> 请妥善保管你的 URL、Token、Encoding-AESKey，切勿泄露给他人。

---

## 消息推送

## 通用字段说明

所有消息类型的回调均包含以下通用字段：

```json
{
    "msgid": "MSGID",
    "aibotid": "AIBOTID",
    "chatid": "CHATID",
    "chattype": "group",
    "from": {
        "userid": "USERID"
    },
    "response_url": "RESPONSEURL",
    "msgtype": "msgtype_name",
    "msgtype_name": {}
}
```

| 参数 | 说明 |
|---|---|
| msgid | 本次回调的唯一性标志，开发者需据此进行事件排重（可能因网络等原因重复回调）|
| aibotid | 智能机器人 id |
| chatid | 会话 id，仅群聊类型时返回 |
| chattype | 会话类型，`single`\`group`，分别表示：单聊\群聊 |
| from.userid | 操作者的 userid。如果智能机器人创建者为企业超级管理员，则为明文 userid，否则为企业主体下的加密 userid |
| response_url | 支持主动回复消息的临时 [url](#62166) |
| msgtype | 消息类型，同时也是消息内容的字段名。取值范围：[text](#文本消息)、[image](#图片消息)、[mixed](#图文混排消息)、[voice](#语音消息)、[file](#文件消息)、[video](#视频消息) |
| msgtype_name | 消息内容结构体，字段名与 msgtype 的值一致，例如 msgtype 为 `text` 时，消息内容字段名为 `text` |

---

## 文本消息

```json
{
    "msgid": "CAIQ16HMjQYY\/NGagIOAgAMgq4KM0AI=",
    "aibotid": "AIBOTID",
    "chatid": "CHATID",
    "chattype": "group",
    "from": {
        "userid": "USERID"
    },
    "response_url": "RESPONSEURL",
    "msgtype": "text",
    "text": {
        "content": "@RobotA hello robot"
    },
    "quote": {
        "msgtype": "text",
        "text": {
            "content": "这是今日的测试情况"
        }
    }
}
```

| 参数 | 说明 |
|---|---|
| msgtype | 固定是 `text` |
| text | 文本消息内容，参考[文本](#文本)结构体说明 |
| quote | 引用内容，若用户引用了其他消息则有该字段，参考[引用](#引用)结构体说明 |

## 图片消息

>仅支持单聊

```json
{
    "msgid": "CAIQz7\/MjQYY\/NGagIOAgAMgl8jK\/gI=",
    "aibotid": "AIBOTID",
    "chattype": "single",
    "from": {
        "userid": "USERID"
    },
    "response_url": "RESPONSEURL",
    "msgtype": "image",
    "image": {
        "url": "IMAGE_URL"
    }
}
```

| 参数 | 说明 |
|---|---|
| msgtype | 固定是 `image` |
| image | 图片内容，参考[图片](#图片)结构体说明 |

## 图文混排消息

```json
{
    "msgid": "CAIQrcjMjQYY\/NGagIOAgAMg6PDc\/w0=",
    "aibotid": "AIBOTID",
    "chatid": "CHATID",
    "chattype": "group",
    "from": {
        "userid": "USERID"
    },
    "response_url": "RESPONSEURL",
    "msgtype": "mixed",
    "mixed": {
        "msg_item": [
            {
                "msgtype": "text",
                "text": {
                    "content": "@机器人 这是今日的测试情况"
                }
            },
            {
                "msgtype": "image",
                "image": {
                    "url": "IMAGE_URL"
                }
            }
        ]
    },
    "quote": {
        "msgtype": "text",
        "text": {
            "content": "这是今日的测试情况"
        }
    }
}
```

| 参数 | 说明 |
|---|---|
| msgtype | 固定是 `mixed` |
| mixed | 图文混排内容，参考[图文混排](#图文混排)结构体说明 |
| quote | 引用内容，若用户引用了其他消息则有该字段，参考[引用](#引用)结构体说明 |

## 语音消息

> 仅支持单聊

```json
{
    "msgid": "CAIQrcjMjQYY\/NGagIOAgAMg6PDc\/w0=",
    "aibotid": "AIBOTID",
    "chattype": "single",
    "from": {
        "userid": "USERID"
    },
    "response_url": "RESPONSEURL",
    "msgtype": "voice",
    "voice": {
        "content": "这是语音转成文本的内容"
    }
}
```

| 参数 | 说明 |
|---|---|
| msgtype | 固定是 `voice` |
| voice | 语音内容，参考[语音](#语音)结构体说明 |

## 文件消息

> 仅支持单聊。智能机器人目前仅支持 **100M** 大小以内的文件回调。

```json
{
    "msgid": "CAIQrcjMjQYY\/NGagIOAgAMg6PDc\/w0=",
    "aibotid": "AIBOTID",
    "chattype": "single",
    "from": {
        "userid": "USERID"
    },
    "response_url": "RESPONSEURL",
    "msgtype": "file",
    "file": {
        "url": "FILE_URL"
    }
}
```

| 参数 | 说明 |
|---|---|
| msgtype | 固定是 `file` |
| file | 参考[文件](#文件)结构体说明 |

## 视频消息

> 仅支持单聊。智能机器人目前仅支持 **100M** 大小以内的视频回调。

```json
{
    "msgid": "CAIQrcjMjQYY\/NGagIOAgAMg6PDc\/w0=",
    "aibotid": "AIBOTID",
    "chattype": "single",
    "from": {
        "userid": "USERID"
    },
    "response_url": "RESPONSEURL",
    "msgtype": "video",
    "video": {
        "url": "VIDEO_URL"
    }
}
```

| 参数 | 说明 |
|---|---|
| msgtype | 固定是 `video` |
| video | 参考[视频](#视频)结构体说明 |

---

## 流式消息刷新

当开发者首次回复流式消息后，企业微信会持续向回调 URL 推送流式消息刷新事件，直到开发者回复 `finish=true`。

```json
{
    "msgid": "CAIQz7\/MjQYY\/NGagIOAgAMgl8jK\/gI=",
    "aibotid": "AIBOTID",
    "chatid": "CHATID",
    "chattype": "group",
    "from": {
        "userid": "USERID"
    },
    "msgtype": "stream",
    "stream": {
        "id": "STREAMID"
    }
}
```

**参数说明：**

| 参数 | 说明 |
|---|---|
| msgid | 本次回调的唯一性标志，开发者需据此进行事件排重（可能因网络等原因重复回调）|
| aibotid | 智能机器人 id |
| chatid | 会话 id，仅群聊类型时返回 |
| chattype | 会话类型，`single`\`group`，分别表示：单聊\群聊 |
| from.userid | 操作者的 userid |
| msgtype | 固定是 `stream`。**该消息事件仅支持[流式消息回复](#62165/流式消息回复)** |
| stream.id | 流式消息的 id，开发者根据该 id 返回对应的流式消息更新内容 |

---

## 结构体说明

## 文本

```json
{
    "content": "@RobotA hello robot"
}
```

| 参数 | 说明 |
|---|---|
| content | 文本消息内容 |

## 图片

```json
{
    "url": "IMAGE_URL"
}
```

| 参数 | 说明 |
|---|---|
| url | 图片的下载 url，该 url 五分钟内有效。文件已加密，解密方式参考[多媒体资源解密](#多媒体资源解密) |

## 图文混排

```json
{
    "msg_item": [
        {
            "msgtype": "text",
            "text": {
                "content": "@机器人 这是今日的测试情况"
            }
        },
        {
            "msgtype": "image",
            "image": {
                "url": "IMAGE_URL"
            }
        }
    ]
}
```

| 参数 | 说明 |
|---|---|
| msg_item.msgtype | 图文混排中的类型，`text`/`image`，分别表示文本和图片 |
| msg_item.text | 图文混排中的文本内容，参考[文本](#文本)结构体说明 |
| msg_item.image | 图文混排中的图片内容，参考[图片](#图片)结构体说明 |

## 语音

```json
{
    "content": "这是语音转成文本的内容"
}
```

| 参数 | 说明 |
|---|---|
| content | 语音转换成文本的内容 |

## 文件

```json
{
    "url": "FILE_URL"
}
```

| 参数 | 说明 |
|---|---|
| url | 文件的下载 url，该 url 五分钟内有效。文件已加密，解密方式参考[多媒体资源解密](#多媒体资源解密) |

## 视频

```json
{
    "url": "VIDEO_URL"
}
```

| 参数 | 说明 |
|---|---|
| url | 视频的下载 url，该 url 五分钟内有效。文件已加密，解密方式参考[多媒体资源解密](#多媒体资源解密) |

## 引用

```json
{
    "msgtype": "text",
    "text": {
        "content": "这是今日的测试情况"
    },
    "image": {
        "url": "IMAGE_URL"
    },
    "mixed": {
        "msg_item": []
    },
    "voice": {
        "content": "这是语音转成文本的内容"
    },
    "file": {
        "url": "FILE_URL"
    },
    "video": {
        "url": "VIDEO_URL"
    }
}
```

| 参数 | 说明 |
|---|---|
| msgtype | 引用的类型，`text`/`image`/`mixed`/`voice`/`file`/`video` |
| text | 引用的文本内容，参考[文本](#文本)结构体说明 |
| image | 引用的图片内容，参考[图片](#图片)结构体说明 |
| mixed | 引用的图文混排内容，参考[图文混排](#图文混排)结构体说明 |
| voice | 引用的语音内容，参考[语音](#语音)结构体说明 |
| file | 引用的文件内容，参考[文件](#文件)结构体说明 |
| video | 引用的视频内容，参考[视频](#视频)结构体说明 |

---

## 多媒体资源解密

图片、文件、视频回调中的 `url` 下载到的资源均经过加密，不能直接打开，需要先解密后使用。

**加密方式说明：**

-算法：AES-256-CBC
- 密钥（AESKey）：与[回调加解密的 Encoding-AESKey](#62162) 相同，Base64 解码后得到 32 字节 AESKey
- IV：取 AESKey 前 16 字节
- 填充：PKCS#7，填充至 32 字节的倍数

**Python 解密示例：**

```python
import base64
from Crypto.Cipher import AES

def decrypt_media(encoding_aes_key, encrypted_data):
    """
    解密多媒体资源
    :param encoding_aes_key: 管理后台配置的 Encoding-AESKey（字符串）
    :param encrypted_data: 下载到的加密文件内容（bytes）
    :return: 解密后的文件内容（bytes）
    """
    # Base64 解码得到 32 字节 AESKey
    aes_key = base64.b64decode(encoding_aes_key + '=')
    # IV 取 AESKey 前 16 字节
    iv = aes_key[:16]
    # AES-256-CBC 解密
    cipher = AES.new(aes_key, AES.MODE_CBC, iv)
    decrypted = cipher.decrypt(encrypted_data)
    #去除 PKCS#7 填充
    pad_len = decrypted[-1]
    return decrypted[:-pad_len]


## 使用示例
import requests

encoding_aes_key = "YOUR_ENCODING_AES_KEY"  # 管理后台配置的 Encoding-AESKey
url = "MEDIA_URL"                            # 回调中的 url 字段

## 下载加密文件
resp = requests.get(url)
if resp.status_code != 200:
    raise Exception(f"下载失败（status={resp.status_code}），媒体文件链接可能已过期，请重新获取")

## 解密
raw_data = decrypt_media(encoding_aes_key, resp.content)
```

>依赖：`pip install pycryptodome requests`
