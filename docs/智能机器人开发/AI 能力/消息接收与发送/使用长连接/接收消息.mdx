---
title: "接收消息"
generated_at: "2026-08-18 16:58:11"
update_time: 1786544595
source: "https://developer.work.weixin.qq.com/document/path/101834"
---

[TOC]

## 概述

用户向智能机器人发送消息时，企业微信会通过长连接推送消息回调（`aibot_msg_callback`）。

与接收 URL 方式的主要差异：
- 回调消息为**明文**，无需解密
- 多媒体资源（图片/文件/视频）结构体中额外返回 `aeskey` 字段，每个下载链接对应唯一的 aeskey，不同于接收 URL 方式使用统一的 Encoding-AESKey

---

## 消息推送

## 通用字段说明

所有消息类型的回调均包含以下通用字段：

```json
{
    "cmd": "aibot_msg_callback",
    "headers": {
        "req_id": "REQUEST_ID"
    },
    "body": {
        "msgid": "MSGID",
        "aibotid": "AIBOTID",
        "chatid": "CHATID",
        "chattype": "group",
        "from": {
            "userid": "USERID"
        },
        "msgtype": "msgtype_name",
        "msgtype_name": {}
    }
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| cmd | string | 固定值 `aibot_msg_callback` |
| headers.req_id | string | 请求唯一标识，**回复消息时需透传此值** |
| body.msgid | string | 本次回调的唯一性标志，用于事件排重（可能因网络等原因重复回调）|
| body.aibotid | string | 智能机器人 BotID |
| body.chatid | string | 会话 id，仅群聊类型时返回 |
| body.chattype | string | 会话类型，`single` 单聊 / `group` 群聊 |
| body.from.userid | string | 消息发送者的userid。如果智能机器人创建者为企业超级管理员，则为明文 userid，否则为企业主体下的加密 userid |
| body.msgtype | string | 消息类型，同时也是消息内容的字段名。取值：[text](#文本消息)、[image](#图片消息)、[mixed](#图文混排消息)、[voice](#语音消息)、[file](#文件消息)、[video](#视频消息) |
| body.msgtype_name | object | 消息内容结构体，字段名与 msgtype 值一致 |

---

## 文本消息

```json
{
    "cmd": "aibot_msg_callback",
    "headers": {
        "req_id": "REQUEST_ID"
    },
    "body": {
        "msgid": "MSGID",
        "aibotid": "AIBOTID",
        "chatid": "CHATID",
        "chattype": "group",
        "from": {
            "userid": "USERID"
        },
        "msgtype": "text",
        "text": {
            "content": "@RobotA hello robot"
        },
        "quote": {
            "msgtype": "text",
            "text": {
                "content": "这是今日的测试情况"
            }
        }
    }
}
```

| 参数 | 说明 |
|---|---|
| body.msgtype | 固定是 `text` |
| body.text | 文本消息内容，参考[文本](#文本)结构体说明 |
| body.quote | 引用内容，若用户引用了其他消息则有该字段，参考[引用](#引用)结构体说明 |

---

## 图片消息

>仅支持单聊

```json
{
    "cmd": "aibot_msg_callback",
    "headers": {
        "req_id": "REQUEST_ID"
    },
    "body": {
        "msgid": "MSGID",
        "aibotid": "AIBOTID",
        "chattype": "single",
        "from": {
            "userid": "USERID"
        },
        "msgtype": "image",
        "image": {
            "url": "IMAGE_URL",
            "aeskey": "AESKEY"
        }
    }
}
```

| 参数 | 说明 |
|---|---|
| body.msgtype | 固定是 `image` |
| body.image | 图片内容，参考[图片](#图片)结构体说明 |

---

## 图文混排消息

```json
{
    "cmd": "aibot_msg_callback",
    "headers": {
        "req_id": "REQUEST_ID"
    },
    "body": {
        "msgid": "MSGID",
        "aibotid": "AIBOTID",
        "chatid": "CHATID",
        "chattype": "group",
        "from": {
            "userid": "USERID"
        },
        "msgtype": "mixed",
        "mixed": {
            "msg_item": [
                {
                    "msgtype": "text",
                    "text": {
                        "content": "@机器人 这是今日的测试情况"
                    }
                },
                {
                    "msgtype": "image",
                    "image": {
                        "url": "IMAGE_URL",
                        "aeskey": "AESKEY"
                    }
                }
            ]
        },
        "quote": {
            "msgtype": "text",
            "text": {
                "content": "这是今日的测试情况"
            }
        }
    }
}
```

| 参数 | 说明 |
|---|---|
| body.msgtype | 固定是 `mixed` |
| body.mixed | 图文混排内容，参考[图文混排](#图文混排)结构体说明 |
| body.quote | 引用内容，若用户引用了其他消息则有该字段，参考[引用](#引用)结构体说明 |

---

## 语音消息

> 仅支持单聊

```json
{
    "cmd": "aibot_msg_callback",
    "headers": {
        "req_id": "REQUEST_ID"
    },
    "body": {
        "msgid": "MSGID",
        "aibotid": "AIBOTID",
        "chattype": "single",
        "from": {
            "userid": "USERID"
        },
        "msgtype": "voice",
        "voice": {
            "content": "这是语音转成文本的内容"
        }
    }
}
```

| 参数 | 说明 |
|---|---|
| body.msgtype | 固定是 `voice` |
| body.voice | 语音内容，参考[语音](#语音)结构体说明 |

---

## 文件消息

> 仅支持单聊。智能机器人目前仅支持 **100M** 大小以内的文件回调。

```json
{
    "cmd": "aibot_msg_callback",
    "headers": {
        "req_id": "REQUEST_ID"
    },
    "body": {
        "msgid": "MSGID",
        "aibotid": "AIBOTID",
        "chattype": "single",
        "from": {
            "userid": "USERID"
        },
        "msgtype": "file",
        "file": {
            "url": "FILE_URL",
            "aeskey": "AESKEY"
        }
    }
}
```

| 参数 | 说明 |
|---|---|
| body.msgtype | 固定是 `file` |
| body.file | 参考[文件](#文件)结构体说明 |

---

## 视频消息

> 仅支持单聊。智能机器人目前仅支持 **100M** 大小以内的视频回调。

```json
{
    "cmd": "aibot_msg_callback",
    "headers": {
        "req_id": "REQUEST_ID"
    },
    "body": {
        "msgid": "MSGID",
        "aibotid": "AIBOTID",
        "chattype": "single",
        "from": {
            "userid": "USERID"
        },
        "msgtype": "video",
        "video": {
            "url": "VIDEO_URL",
            "aeskey": "AESKEY"
        }
    }
}
```

| 参数 | 说明 |
|---|---|
| body.msgtype | 固定是 `video` |
| body.video | 参考[视频](#视频)结构体说明 |

---

## 结构体说明

## 文本

```json
{
    "content": "@RobotA hello robot"
}
```

| 参数 | 说明 |
|---|---|
| content | 文本消息内容 |

## 图片

```json
{
    "url": "IMAGE_URL",
    "aeskey": "AESKEY"
}
```

| 参数 | 说明 |
|---|---|
| url | 图片的下载 url，该 url 五分钟内有效。文件已加密，解密方式参考[多媒体资源解密](#多媒体资源解密) |
| aeskey | 该下载链接对应的解密密钥，每个 url 的aeskey 唯一 |

## 图文混排

```json
{
    "msg_item": [
        {
            "msgtype": "text",
            "text": { "content": "@机器人 这是今日的测试情况" }
        },
        {
            "msgtype": "image",
            "image": {
                "url": "IMAGE_URL",
                "aeskey": "AESKEY"
            }
        }
    ]
}
```

| 参数 | 说明 |
|---|---|
| msg_item.msgtype | 图文混排中的类型，`text`/`image` |
| msg_item.text | 图文混排中的文本内容，参考[文本](#文本)结构体说明 |
| msg_item.image | 图文混排中的图片内容，参考[图片](#图片)结构体说明 |

## 语音

```json
{
    "content": "这是语音转成文本的内容"
}
```

| 参数 | 说明 |
|---|---|
| content | 语音转换成文本的内容 |

## 文件

```json
{
    "url": "FILE_URL",
    "aeskey": "AESKEY"
}
```

| 参数 | 说明 |
|---|---|
| url | 文件的下载 url，该 url 五分钟内有效。文件已加密，解密方式参考[多媒体资源解密](#多媒体资源解密) |
| aeskey | 该下载链接对应的解密密钥，每个 url 的 aeskey 唯一 |

## 视频

```json
{
    "url": "VIDEO_URL",
    "aeskey": "AESKEY"
}
```

| 参数 | 说明 |
|---|---|
| url | 视频的下载 url，该 url 五分钟内有效。文件已加密，解密方式参考[多媒体资源解密](#多媒体资源解密) |
| aeskey | 该下载链接对应的解密密钥，每个 url 的 aeskey 唯一 |

## 引用

```json
{
    "msgtype": "text",
    "text": {
        "content": "这是今日的测试情况"
    },
    "image": {
        "url": "IMAGE_URL",
        "aeskey": "AESKEY"
    },
    "mixed": {
        "msg_item": [
            {
                "msgtype": "text",
                "text": {
                    "content": "@机器人 这是今日的测试情况"
                }
            },
            {
                "msgtype": "image",
                "image": {
                    "url": "IMAGE_URL",
                    "aeskey": "AESKEY"
                }
            }
        ]
    },
    "voice": {
        "content": "这是语音转成文本的内容"
    },
    "file": {
        "url": "FILE_URL",
        "aeskey": "AESKEY"
    },
    "video": {
        "url": "VIDEO_URL",
        "aeskey": "AESKEY"
    }
}
```

| 参数 | 说明 |
|---|---|
| msgtype | 引用的类型，`text`/`image`/`mixed`/`voice`/`file`/`video` |
| text | 引用的文本内容，参考[文本](#文本)结构体说明 |
| image | 引用的图片内容，参考[图片](#图片)结构体说明 |
| mixed | 引用的图文混排内容，参考[图文混排](#图文混排)结构体说明 |
| voice | 引用的语音内容，参考[语音](#语音)结构体说明 |
| file | 引用的文件内容，参考[文件](#文件)结构体说明 |
| video | 引用的视频内容，参考[视频](#视频)结构体说明 |

---

## 多媒体资源解密

图片、文件、视频回调中的`url` 下载到的资源均经过加密，需要使用对应的 `aeskey` 解密后使用。

**与接收 URL 方式的区别**：接收 URL 方式使用统一的 Encoding-AESKey 解密所有多媒体资源；长连接方式每个 url 对应唯一的 `aeskey`，直接从回调中获取，无需配置固定密钥。

**加密方式说明：**

- 算法：AES-256-CBC
- 密钥：回调中的 `aeskey` 字段（32 字节，直接使用，无需 Base64 解码）
- IV：取aeskey 前 16 字节
- 填充：PKCS#7，填充至 32 字节的倍数

**Python 解密示例：**

```python
import requests
from Crypto.Cipher import AES

def decrypt_media(aeskey, encrypted_data):
    """
    解密长连接模式下的多媒体资源
    :param aeskey: 回调中返回的 aeskey 字段（32 字节 UTF-8 字符串，直接用作密钥，无需 base64 解码）
    :param encrypted_data: 下载到的加密文件内容（bytes）
    :return: 解密后的文件内容（bytes）
    """
    # aeskey 直接作为密钥（32 字节），IV 取前 16 字节
    key = aeskey.encode('utf-8')
    iv = key[:16]
    # AES-256-CBC 解密
    cipher = AES.new(key, AES.MODE_CBC, iv)
    decrypted = cipher.decrypt(encrypted_data)
    # 去除 PKCS#7 填充
    pad_len = decrypted[-1]
    return decrypted[:-pad_len]


## 使用示例（以图片为例）
aeskey = "AESKEY"   # 回调 body.image.aeskey
url = "IMAGE_URL"   # 回调 body.image.url

## 下载加密文件
resp = requests.get(url)
if resp.status_code != 200:
    raise Exception(f"下载失败（status={resp.status_code}），媒体文件链接可能已过期，请重新获取")

## 解密
raw_data = decrypt_media(aeskey, resp.content)
```

> 依赖：`pip install pycryptodome requests`
